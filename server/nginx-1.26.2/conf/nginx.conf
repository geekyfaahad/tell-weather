# user  nobody;
worker_processes  auto;

events {
    worker_connections  1024;
}


http {
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;
    #  proxy_cache_path C:/Users/geeky/Downloads/Compressed/tell-weather-main/server/nginx-1.26.2/cache levels=1:2 keys_zone=static_cache:10m max_size=1g inactive=60m use_temp_path=off;
    server {
        listen       88;
        location /static {
        proxy_pass http://127.0.0.1:5000;
        proxy_cache static_cache;
        proxy_cache_valid 200 1h;
        proxy_cache_use_stale error timeout updating;
        }
        # server_name  localhost;
        location / {
        proxy_pass http://127.0.0.1:5000;
        client_max_body_size 100000M;
        limit_req zone=mylimit burst=20 nodelay;
        proxy_buffering on;
        proxy_buffers 16 4k;
        proxy_buffer_size 8k;

        }
        }
    }




# new code from chatgpt

# worker_processes  auto;

# events {
#     worker_connections  1024;
# }

# http {
#     # Updated cache path
#     proxy_cache_path C:/nginx/cache levels=1:2 keys_zone=static_cache:10m max_size=1g inactive=60m use_temp_path=off;

#     # Other settings
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

#     limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;

#     server {
#         listen 88;

#         location /static {
#             proxy_pass http://127.0.0.1:5000;
#             proxy_cache static_cache;
#             proxy_cache_valid 200 1h;
#             proxy_cache_use_stale error timeout updating;
#         }

#         location / {
#             limit_req zone=mylimit burst=20 nodelay;
#             proxy_pass http://127.0.0.1:5000;
#             client_max_body_size 100000M;

#             proxy_buffering on;
#             proxy_buffers 16 4k;
#             proxy_buffer_size 8k;
#         }

#         error_page 404 /custom_404.html;
#         location = /custom_404.html {
#             root /usr/share/nginx/html;
#             internal;
#         }

#         error_page 500 502 503 504 /custom_50x.html;
#         location = /custom_50x.html {
#             root /usr/share/nginx/html;
#             internal;
#         }
#     }

#     keepalive_timeout 65;
#     client_header_timeout 15;
#     client_body_timeout 15;
#     send_timeout 15;
# }

